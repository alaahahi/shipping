# خطة عمل نظام الترخيص والتفعيل للمنتج

## 📋 نظرة عامة

نظام ترخيص متكامل لحماية المنتج مع:
- ✅ مفتاح تفعيل فريد لكل تثبيت
- ✅ تشفير الكود الحساس
- ✅ التحقق من الترخيص عند كل طلب (اختياري)
- ✅ دعم التفعيل Online/Offline
- ✅ حماية من النسخ غير المصرح بها

---

## 🎯 الأهداف

1. **حماية المنتج** من الاستخدام غير المصرح به
2. **تتبع التثبيتات** وربطها بمفتاح تفعيل
3. **تشفير الكود الحساس** (مفاتيح API، إعدادات مهمة)
4. **سهولة التفعيل** للمستخدم النهائي
5. **مراقبة الترخيص** وإدارة التثبيتات

---

## 📦 المكتبات المقترحة

### 1. Laravel License Manager (مخصص)
**الوصف:** نظام ترخيص مخصص مبني على Laravel
**المميزات:**
- ✅ مجاني ومفتوح المصدر
- ✅ قابل للتخصيص بالكامل
- ✅ يعمل مع Laravel Encryption
- ✅ لا يحتاج مكتبات خارجية

**التكلفة:** مجاني

### 2. Laravel Encryption (مدمج)
**الوصف:** نظام التشفير المدمج في Laravel
**المميزات:**
- ✅ مدمج في Laravel
- ✅ آمن وقوي
- ✅ سهل الاستخدام

**التكلفة:** مجاني

### 3. IonCube Encoder (اختياري - مدفوع)
**الوصف:** تشفير PHP Code
**المميزات:**
- ✅ تشفير قوي للكود
- ✅ صعب الاختراق
- ✅ حماية كاملة للملفات

**التكلفة:** ~$199/year
**الموقع:** https://www.ioncube.com/

### 4. SourceGuardian (اختياري - مدفوع)
**الوصف:** بديل IonCube
**المميزات:**
- ✅ تشفير قوي
- ✅ أداء أفضل من IonCube
- ✅ دعم أفضل

**التكلفة:** ~$149/year
**الموقع:** https://sourceguardian.com/

---

## 🏗️ هيكل النظام المقترح

```
app/
├── Models/
│   └── License.php              # نموذج الترخيص
├── Services/
│   ├── LicenseService.php       # خدمة إدارة الترخيص
│   └── CodeEncryptionService.php # خدمة تشفير الكود
├── Http/
│   ├── Controllers/
│   │   └── LicenseController.php # تحكم في الترخيص
│   └── Middleware/
│       └── CheckLicense.php     # Middleware للتحقق
├── Console/
│   └── Commands/
│       └── GenerateLicense.php  # أمر إنشاء تراخيص
└── Helpers/
    └── LicenseHelper.php        # دوال مساعدة

database/
└── migrations/
    └── create_licenses_table.php

config/
└── license.php                  # إعدادات الترخيص
```

---

## 🔐 آلية العمل

### 1. توليد مفتاح الترخيص

**المعلومات المطلوبة:**
- Domain/IP Address
- Server Fingerprint (MAC Address, CPU ID, etc.)
- Installation Date
- License Type (Trial, Standard, Premium)
- Expiry Date

**التشفير:**
```php
$licenseData = [
    'domain' => 'system.intellijapp.com',
    'fingerprint' => 'unique-server-id',
    'type' => 'standard',
    'expires_at' => '2025-12-31',
    'issued_at' => now(),
    'max_installations' => 1
];

$licenseKey = encrypt(json_encode($licenseData));
```

### 2. ملف الترخيص

**الموقع:** `storage/app/license.key`
**التنسيق:** JSON مشفر

```json
{
    "license_key": "encrypted-key-here",
    "domain": "system.intellijapp.com",
    "fingerprint": "server-unique-id",
    "activated_at": "2024-01-01 00:00:00",
    "expires_at": "2025-12-31 23:59:59",
    "type": "standard"
}
```

### 3. التحقق من الترخيص

**عند كل طلب (اختياري):**
```php
// في Middleware
if (!LicenseService::isValid()) {
    return redirect()->route('license.activate');
}
```

**عند بدء التطبيق:**
```php
// في AppServiceProvider
if (!LicenseService::check()) {
    abort(403, 'License not activated');
}
```

---

## 📝 خطوات التنفيذ

### المرحلة 1: إعداد البنية الأساسية (يوم 1)

1. ✅ إنشاء Migration للجدول `licenses`
2. ✅ إنشاء Model `License`
3. ✅ إنشاء Service `LicenseService`
4. ✅ إنشاء Config `config/license.php`
5. ✅ إنشاء Helper `LicenseHelper`

### المرحلة 2: نظام التفعيل (يوم 2)

1. ✅ إنشاء Controller `LicenseController`
2. ✅ إنشاء Routes للتفعيل
3. ✅ إنشاء صفحة التفعيل (Inertia)
4. ✅ إنشاء API للتحقق من الترخيص
5. ✅ دعم التفعيل Online/Offline

### المرحلة 3: الحماية (يوم 3)

1. ✅ إنشاء Middleware `CheckLicense`
2. ✅ تطبيق Middleware على Routes المهمة
3. ✅ إضافة التحقق في AppServiceProvider
4. ✅ إنشاء Command للتحقق الدوري

### المرحلة 4: التشفير (يوم 4)

1. ✅ تشفير المفاتيح الحساسة في `.env`
2. ✅ تشفير الكود الحساس (API Keys, etc.)
3. ✅ إنشاء Service للتشفير
4. ✅ إضافة حماية للملفات المهمة

### المرحلة 5: واجهة الإدارة (يوم 5)

1. ✅ صفحة إدارة الترخيص
2. ✅ عرض حالة الترخيص
3. ✅ تجديد الترخيص
4. ✅ سجل التفعيلات

---

## 🔒 خيارات التشفير

### الخيار 1: Laravel Encryption (مجاني - موصى به)

**المميزات:**
- ✅ مجاني ومدمج
- ✅ آمن جداً
- ✅ سهل الاستخدام
- ✅ لا يحتاج إضافات

**الاستخدام:**
```php
// تشفير
$encrypted = encrypt($data);

// فك التشفير
$decrypted = decrypt($encrypted);
```

### الخيار 2: IonCube Encoder (مدفوع)

**المميزات:**
- ✅ تشفير قوي للكود
- ✅ صعب الاختراق
- ✅ حماية كاملة

**العيوب:**
- ❌ يحتاج IonCube Loader على السيرفر
- ❌ تكلفة إضافية
- ❌ صعوبة في التطوير

### الخيار 3: Hybrid (موصى به)

**الجمع بين:**
- Laravel Encryption للمفاتيح والبيانات
- Obfuscation للكود الحساس (PHP Obfuscator مجاني)
- IonCube للكود الحساس جداً (اختياري)

---

## 🎨 واجهة المستخدم

### صفحة التفعيل

```
┌─────────────────────────────────────┐
│   تفعيل المنتج                      │
├─────────────────────────────────────┤
│                                     │
│   مفتاح الترخيص:                    │
│   [________________________]        │
│                                     │
│   معلومات السيرفر:                  │
│   Domain: system.intellijapp.com    │
│   Fingerprint: xxxxx-xxxxx-xxxxx    │
│                                     │
│   [تفعيل الآن]  [تفعيل لاحقاً]      │
│                                     │
└─────────────────────────────────────┘
```

### صفحة حالة الترخيص

```
┌─────────────────────────────────────┐
│   حالة الترخيص                      │
├─────────────────────────────────────┤
│                                     │
│   ✅ الترخيص مفعل                    │
│   النوع: Standard                   │
│   ينتهي في: 31/12/2025              │
│   المتبقي: 365 يوم                  │
│                                     │
│   [تجديد الترخيص]                   │
│                                     │
└─────────────────────────────────────┘
```

---

## 🔄 سيناريوهات الاستخدام

### السيناريو 1: التثبيت الأول

1. المستخدم يرفع المشروع على السيرفر
2. يفتح الموقع لأول مرة
3. يتم توجيهه لصفحة التفعيل
4. يدخل مفتاح الترخيص
5. النظام يتحقق من المفتاح
6. يتم حفظ الترخيص
7. التطبيق يعمل بشكل طبيعي

### السيناريو 2: التفعيل Offline

1. المستخدم يطلب مفتاح تفعيل
2. يرسل معلومات السيرفر (Domain, Fingerprint)
3. يتم توليد مفتاح مخصص
4. المستخدم يدخل المفتاح
5. النظام يتحقق محلياً
6. التفعيل يتم

### السيناريو 3: انتهاء الترخيص

1. النظام يكتشف انتهاء الترخيص
2. يعرض رسالة تحذيرية
3. يمنع الوصول للميزات المهمة
4. يسمح بالوصول للواجهة فقط
5. المستخدم يجدد الترخيص

---

## 📊 قاعدة البيانات

### جدول `licenses`

```sql
CREATE TABLE licenses (
    id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    license_key TEXT NOT NULL,
    domain VARCHAR(255) NOT NULL,
    fingerprint VARCHAR(255) NOT NULL,
    type ENUM('trial', 'standard', 'premium') DEFAULT 'standard',
    max_installations INT DEFAULT 1,
    activated_at TIMESTAMP NULL,
    expires_at TIMESTAMP NULL,
    is_active BOOLEAN DEFAULT TRUE,
    last_verified_at TIMESTAMP NULL,
    created_at TIMESTAMP NULL,
    updated_at TIMESTAMP NULL,
    
    UNIQUE KEY unique_domain (domain),
    INDEX idx_fingerprint (fingerprint),
    INDEX idx_expires_at (expires_at)
);
```

---

## 🛡️ الحماية المقترحة

### 1. حماية Routes

```php
Route::middleware(['auth', 'check.license'])->group(function () {
    // جميع Routes المهمة
});
```

### 2. حماية Controllers

```php
public function __construct()
{
    $this->middleware('check.license');
}
```

### 3. التحقق الدوري

```php
// في Kernel.php
$schedule->command('license:verify')
    ->hourly()
    ->withoutOverlapping();
```

### 4. تشفير الملفات الحساسة

- `.env` - استخدام Laravel Encryption
- API Keys - تشفير في Database
- License Keys - تشفير في Storage

---

## 📦 الملفات المطلوبة

### 1. Core Files
- `app/Models/License.php`
- `app/Services/LicenseService.php`
- `app/Http/Controllers/LicenseController.php`
- `app/Http/Middleware/CheckLicense.php`
- `app/Console/Commands/GenerateLicense.php`
- `app/Console/Commands/VerifyLicense.php`
- `app/Helpers/LicenseHelper.php`
- `config/license.php`

### 2. Database
- `database/migrations/xxxx_create_licenses_table.php`

### 3. Views (Inertia)
- `resources/js/Pages/License/Activate.vue`
- `resources/js/Pages/License/Status.vue`
- `resources/js/Pages/License/Renew.vue`

### 4. Routes
- `routes/web.php` - Routes التفعيل
- `routes/api.php` - API التحقق

---

## ⚙️ الإعدادات

### config/license.php

```php
return [
    'enabled' => env('LICENSE_ENABLED', true),
    'check_on_every_request' => env('LICENSE_CHECK_EVERY_REQUEST', false),
    'grace_period_days' => env('LICENSE_GRACE_PERIOD', 7),
    'verification_interval' => env('LICENSE_VERIFICATION_INTERVAL', 3600), // seconds
    'offline_mode' => env('LICENSE_OFFLINE_MODE', true),
    'license_file' => storage_path('app/license.key'),
];
```

---

## 🚀 خطة التنفيذ التفصيلية

### اليوم 1: البنية الأساسية
- [ ] Migration للجدول
- [ ] Model License
- [ ] LicenseService الأساسي
- [ ] Config File
- [ ] Helper Functions

### اليوم 2: نظام التفعيل
- [ ] LicenseController
- [ ] Routes
- [ ] صفحة التفعيل (Inertia)
- [ ] API للتحقق
- [ ] دعم Offline

### اليوم 3: الحماية
- [ ] CheckLicense Middleware
- [ ] تطبيق على Routes
- [ ] التحقق في AppServiceProvider
- [ ] Command للتحقق الدوري

### اليوم 4: التشفير
- [ ] تشفير المفاتيح
- [ ] CodeEncryptionService
- [ ] حماية الملفات

### اليوم 5: الواجهة والإدارة
- [ ] صفحة حالة الترخيص
- [ ] صفحة التجديد
- [ ] Dashboard للإدارة

---

## 💡 توصيات إضافية

### 1. License Server (اختياري)

إنشاء سيرفر مركزي لإدارة الترخيصات:
- توليد المفاتيح
- تتبع التثبيتات
- إدارة التجديدات
- إلغاء الترخيصات

### 2. Analytics

تتبع استخدام المنتج:
- عدد المستخدمين
- الميزات المستخدمة
- الأخطاء

### 3. Auto-Updates

نظام تحديثات تلقائية:
- التحقق من التحديثات
- تنزيل التحديثات
- تثبيت التحديثات

---

## ❓ أسئلة للتوضيح

1. **نوع الترخيص المطلوب:**
   - Online فقط؟
   - Offline أيضاً؟
   - Hybrid (Online + Offline)؟

2. **مستوى الحماية:**
   - أساسي (Laravel Encryption فقط)؟
 

3. **التحقق من الترخيص:**
 
   - دوري (كل ساعة/يوم)؟

4. **مدة الترخيص:**
   - دائم؟
   - سنوي؟
   - تجريبي (Trial)؟


---

## ✅ الخطوة التالية

بعد الموافقة على الخطة، سأبدأ بالتنفيذ حسب الأولويات المحددة.

**هل تريد البدء بالتنفيذ الآن؟**

